import React from "react";

const DownloadButton = () => {
  return (
    <button
      type="button"
      className="border-accent text-accent h-9 rounded-lg border-2 px-8 font-bold hover:cursor-pointer"
    >
      Download CV
    </button>
  );
};

export default DownloadButton;
